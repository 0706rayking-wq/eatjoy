# EatJoy GitHub Upload Copy

This folder is a cleaned static upload copy generated from the local project.

Included:
- `index.html`
- `game.js`
- only assets currently referenced by those files

Excluded:
- unused generated UI source sheets
- unused UI slices
- local caches, archives, build folders, and secrets
